export class OrderItem {
    private id:number;

    private quantity: number;

    private idProduct: number;

    constructor(id:number, quantity: number, idProduct: number) {
      this.id = id;
      this.quantity = id;
      this.idProduct = idProduct;
    }
}
